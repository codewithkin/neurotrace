/**
 * Rewarded video gate for the Doctor's Report PDF.
 * AdMob wiring lands in the dedicated ads integration step.
 */
export async function showPdfUnlockAd(): Promise<boolean> {
  // Placeholder until react-native-google-mobile-ads is wired up.
  return true;
}

export async function showTrendExportAd(): Promise<boolean> {
  return true;
}
